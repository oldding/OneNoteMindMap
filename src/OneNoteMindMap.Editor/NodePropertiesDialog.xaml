<Window x:Class="OneNoteMindMap.Editor.NodePropertiesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="520" Height="500"
        MinWidth="440" MinHeight="420"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label x:Name="LblText" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">文本:</Label>
        <TextBox x:Name="NodeTextBox" Grid.Row="0" Grid.Column="1" Margin="0,3"/>

        <Label x:Name="LblIcon" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center">图标:</Label>
        <TextBox x:Name="IconTextBox" Grid.Row="1" Grid.Column="1" Margin="0,3" MaxLength="16"/>

        <Label x:Name="LblColor" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center">颜色:</Label>
        <Grid Grid.Row="2" Grid.Column="1" Margin="0,3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="36"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="ColorTextBox" TextChanged="ColorTextBox_TextChanged"/>
            <Border x:Name="ColorPreview" Grid.Column="1" Margin="6,0" BorderBrush="#999999" BorderThickness="1" CornerRadius="2"/>
            <Button x:Name="BtnClearColor" Grid.Column="2" Padding="8,2" Click="ClearColor_Click">清除</Button>
        </Grid>

        <Label x:Name="LblNote" Grid.Row="3" Grid.Column="0" VerticalAlignment="Top" Margin="0,5,0,0">备注:</Label>
        <TextBox x:Name="NoteTextBox" Grid.Row="3" Grid.Column="1" Margin="0,3"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

        <Label x:Name="LblLink" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center">链接:</Label>
        <TextBox x:Name="LinkTextBox" Grid.Row="4" Grid.Column="1" Margin="0,3"/>

        <TextBlock x:Name="HintText" Grid.Row="5" Grid.Column="1" Margin="0,4,0,8"
                   Foreground="#666666" FontSize="12" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnOk" Width="84" Margin="4,0" Padding="8,4" IsDefault="True" Click="Ok_Click">确定</Button>
            <Button x:Name="BtnCancel" Width="84" Margin="4,0" Padding="8,4" IsCancel="True">取消</Button>
        </StackPanel>
    </Grid>
</Window>
